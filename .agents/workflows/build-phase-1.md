---
description: Build Phase 1 queue, hashing, database, and dry-run pipeline
---

1. Read `TASKS.md` Phase 1 only.
2. Act as `@architect` and confirm interfaces in an artifact.
3. Act as `@backend` and implement only Phase 1.
4. Act as `@qa` and add tests.
5. Run tests.
6. Act as `@security` and inspect logs and secret handling.
7. Update `TASKS.md`, `CHANGELOG.md`, and `artifacts/PHASE_1_REPORT.md`.
8. Do not implement face recognition, scene classification, or Telegram uploads beyond mocks in this phase.
