# Skill: Security

- Load secrets from `.env`.
- Redact tokens, API hashes, GPS coordinates, and face paths from normal logs.
- Verify dependencies before install.
- Treat all incoming filenames and metadata as untrusted.
- Never execute media content.
- Restrict local Bot API listener to localhost unless explicitly secured.
- Keep face data outside synced folders.
